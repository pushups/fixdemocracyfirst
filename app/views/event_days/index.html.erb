<p id="notice"><%= notice %></p>

<h1>Listing Event Days</h1>

<table>
  <thead>
    <tr>
      <th>Rwu</th>
      <th>Event</th>
      <th>Date</th>
      <th>Start time</th>
      <th>End time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_days.each do |event_day| %>
      <tr>
        <td><%= event_day.rwu_id %></td>
        <td><%= event_day.event_id %></td>
        <td><%= event_day.date %></td>
        <td><%= event_day.start_time %></td>
        <td><%= event_day.end_time %></td>
        <td><%= link_to 'Show', event_day %></td>
        <td><%= link_to 'Edit', edit_event_day_path(event_day) %></td>
        <td><%= link_to 'Destroy', event_day, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event day', new_event_day_path %>
